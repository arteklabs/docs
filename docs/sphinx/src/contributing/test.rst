Tests
=====

About
-----

The section shows how to run the unit test coverage over the solution.

Getting Started
---------------

Having cloned the repo and ran ``. init.sh`` to bootstrap your development environment, run:

.. code:: shell

   (dmacli-py3.8) pytest -v

.. code:: shell

   (dmacli-py3.8) pytest -k test_version -v
