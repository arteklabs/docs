Architecture
============

About
-----

The project's architecture specifies the application's integrated components.

Components
----------

* docker container
* python click app

Distribution
------------

The project publishes the python package to PyPi and the docker container to DockerHub. Pushing the
container fetches the latest version of the python package from PyPi.
